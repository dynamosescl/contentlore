{
  "version": 1,
  "description": "Route /api/* and /creator-profile/* through Pages Functions; everything else served as static.",
  "include": [
    "/api/*",
    "/creator-profile/*",
    "/wrapped/*"
  ],
  "exclude": []
}
