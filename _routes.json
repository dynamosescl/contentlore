{
  "version": 1,
  "description": "Route dynamic paths through Functions; everything else served as static.",
  "include": [
    "/api/*",
    "/creator/*",
    "/people",
    "/people/*",
    "/discover",
    "/discover/*",
    "/claim",
    "/admin"
  ],
  "exclude": []
}
